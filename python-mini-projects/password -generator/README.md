# Password Generator

This project is a simple Python program that generates a random password.

The user can specify the desired password length, and the program will create a password using random characters.

## Features

- Random password generation
- Uses letters, numbers, and symbols
- User-defined password length
- Simple command-line interface

## Technologies Used

- Python
- Random module

## Example
